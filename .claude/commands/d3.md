---
description: Tarea D3 — Glosario y terminología única en toda la documentación
---

Tarea D3 del plan. Lee antes de nada CLAUDE.md (sobre todo el apartado 1, política de
nombres y tabla de terminología) y PLAN.md.

Objetivo: fijar el vocabulario del producto y aplicarlo en todo el texto. Hoy conviven
Orden/Expedición/Pedido, Viaje/Ruta, "API Inbox"/"Bandeja de Entrada API", y "Reglas de
Tarifa" significa dos cosas distintas en 4.2.3 y 4.4.3.

Fase 1 — Inventario (solo lectura, enséñamelo antes de tocar nada):
1. Recorre source/17.0/**/*.rst y saca la lista de términos de negocio con sus variantes y
   dónde aparece cada variante (fichero y número de apariciones).
2. Para cada término, verifica en /Users/paco/odoo17/custom_addons/tms_suite/ el modelo Odoo
   y cómo lo llama la UI en español (views/*_menu.xml, tms*/i18n/es.po). El nombre que ve el
   usuario en pantalla es el candidato preferente.
3. Propónme la tabla definitiva: término, definición de una o dos frases, sinónimos que se
   dejan de usar, modelo Odoo. Incluye también los términos del manual del conductor (POD,
   Reserva, KO, Dominio, Reembolso, Incidencia). Espera mi OK.

Fase 2 — Aplicar:
4. Crea source/17.0/glossary.rst con la tabla aprobada (list-table, ordenada alfabéticamente,
   con `.. glossary::` de Sphinx si encaja para poder usar :term:). Engánchalo al toctree de
   17.0/index.rst y a la tarjeta de portada.
5. Sustituye las variantes en todos los .rst por el término fijado. Cuidado con los títulos
   (cambia el subrayado si cambia la longitud), con las etiquetas autosectionlabel y con las
   referencias cruzadas. Los nombres de modelos y las rutas de menú no se tocan.
6. Actualiza la tabla de terminología de CLAUDE.md para que apunte al glosario.
7. Compila con -W. Enséñame el diff por fichero (git diff --stat) y una muestra de cambios.

Cierra con /cerrar-tarea.
