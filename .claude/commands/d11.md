---
description: Tarea D11 — Manual del conductor: limpiar, dividir y completar
---

Tarea D11 del plan. Lee antes de nada CLAUDE.md y PLAN.md. Público: el conductor. Cero
tecnicismos, tono de tú, pasos numerados, una captura por paso donde haga falta.

Puntos concretos detectados el 2026-09-08 en source/17.0/10_manual_app/:
- Tono: 10_1 y 10_2 tutean; 10_3 pasa a tercera persona desde ~174 y a primera persona
  coloquial sin tildes en ~599-617 ("nos iremos encima del bulto en la aplicacion").
- Restos de Word: líneas duplicadas 10_3 ~343-344 y ~356-357; un "---" suelto en ~359;
  viñetas con tabulador y "•" en ~404-405 y "·" en ~203-205; frase cortada en 10_2 ~154;
  etiqueta `.. _section-1:` colgada al final de 10_2; tildes ("álias", "Volúmen").
- Jerarquía RST rota en 10_2 (mete un nivel ~ a mitad) y siete ^ planos en "Reportar
  incidencias" de 10_3.
- Cuatro list-tables con imágenes dentro (10_3 ~287-296, ~440-447, ~459-467, ~844-852): en el
  móvil desbordan. Sustituir por figuras seguidas o `.. grid::`.
- Falta contenido: el flujo de CARGA (recogida) completo (solo está la entrega), el cierre de
  ruta / fin de jornada, el modo sin cobertura y la sincronización (qué pasa si no llega a
  Odoo, botón Limpiar), la navegación, y qué hace "Reiniciar".
- Términos sin explicar: POD, Reserva, KO, Dominio, "sistema de predicción", footer, login.
- Títulos sin numerar (el resto del manual numera) y dos nombres para el mismo manual
  ("10. Manual App Conductor" en index.rst y "Manual del conductor" en layout.html).

Cómo hacerlo:
1. Propón la división de 10_3_report.rst (873 líneas). Punto de partida: detalle de la parada
   / entregar una parada / cargar una parada (nuevo) / reembolsos / incidencias / fin de
   jornada y sin cobertura (nuevo). Ficheros 10_N_slug.rst, imágenes renombradas con git mv
   al prefijo nuevo. Espera mi OK.
2. Para lo que falta por escribir, mira la app: /Users/paco/tms_odoo_app (pantallas, flujos)
   y tms_app en tms_suite (qué acepta el servidor). No inventes pantallas.
3. Reescribe con tono único, glosario al inicio (una tabla de seis términos), pasos
   numerados. Marcadores de captura para las pantallas que faltan, registrados en
   CAPTURAS_PENDIENTES.md (las capturas de la app las hace María desde la app con datos de
   prueba, nunca con datos reales).
4. Un solo nombre: "Manual del conductor", en index.rst, layout.html y la tarjeta de portada.
5. Compila con -W y revisa una página en anchura móvil (el conductor lo lee en el teléfono).

Cierra con /cerrar-tarea. Si no cabe en una sesión, cierra con 🔶 y el fichero por el que
seguir.
